# Phishing URL Quick-Check (Flask + Logistic Regression)

A small demo web app that flags suspicious URLs using a simple ML model trained on tokenized URL text.

## Run locally
```bash
pip install -r requirements.txt
python app.py
